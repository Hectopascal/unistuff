#define DEFAULT_RATE 3

void throwDice (Game g, int diceScore){
    
    
    if(diceScore == 7){
        
    
    
    }
    g->turnCounter++;		
}

int getExchangeRate (Game g, int player, int disciplineFrom, int disciplineTo){
	pID = player -1;
	int exRate;
	
	if(disciplineFrom == 1){
	    if(g->defaultBoard.vertex[9][1] == 1 || g->defaultBoard.vertex[8][1] == 1){
	    
	    
	    } 
	
	    //path bpsCentre = {'R','R','L','L','R','L','R','L','R'};
	    //int retrain = getCampus(Game g, path pathToVertex);
	}else{
	    exRate = DEFAULT_RATE;
	}
	
	return exRate;
}






